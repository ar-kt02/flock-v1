{
  "name": "project-root",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky",
    "test": "cd backend && npm test"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "*.{js,ts}": "eslint --fix"
  }
}
